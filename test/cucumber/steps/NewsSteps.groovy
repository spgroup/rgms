import cucumber.runtime.PendingException;
import static cucumber.api.groovy.EN.*

//You can implement missing steps with the snippets below:
Given(~'^the system has no news with description "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

When(~'^I create a news with description "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Then(~'^the news  with description  "([^"]*)" is properly stored by the system$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Given(~'^the system has a news with description "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

When(~'^I delete a news with description "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Then(~'^the news  with "([^"]*)" doesnt exists$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Then(~'^the news  with "([^"]*)" is not registered$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Given(~'^the research group  in the system has no Twitter account "([^"]*)" associated$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

When(~'^I associate the account "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}

Then(~'^the news can be retrieved by a Twitter post with "([^"]*)"$') { String arg1 ->
	// Express the Regexp above with the code you wish you had
	throw new PendingException()
}